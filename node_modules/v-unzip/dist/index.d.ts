interface UzipOptions {
    recursive: Function;
}
/**
 * 将一个 zip 解压到指定的位置
 * @param source
 * @param dist
 */
export declare function unzip(source: string, dist: string, options?: UzipOptions): Promise<undefined>;
/**
 * 检查是否支持当前平台
 * @param platform
 */
export declare function checkSupportPlatform(platform: string): boolean;
export {};
