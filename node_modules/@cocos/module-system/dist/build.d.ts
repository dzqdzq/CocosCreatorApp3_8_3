interface BuildOptions {
    out: string;
    sourceMap: boolean;
    minify: boolean;
    platform: string;
    hmr?: boolean;
    editor?: boolean;
}
export declare function build({ out, sourceMap, minify, platform, hmr, editor, }: BuildOptions): Promise<void>;
export {};
