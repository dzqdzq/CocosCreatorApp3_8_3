import { babel as Transformer } from "../../../transformer/lib/index";
import BabelFile = Transformer.core.BabelFile;
import t = Transformer.core.types;
export declare class FieldDecoratorHelper {
  private _moduleName;
  private _moduleSource?;
  private _file2NamedIdentifier;
  getModuleName(): string;
  genModuleSource(): string;
  addHelper(file: BabelFile): t.Identifier;
}