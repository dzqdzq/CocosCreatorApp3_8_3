/**
 * 对象回收池
 */
export declare class ObjectRecycler<T extends {
    initialize(): void;
    destroy?(): void;
}> {
    objectPool: T[];
    option: {
        generate(): T;
    };
    constructor(option: {
        generate(): T;
    });
    /**
     * 从对象池获取对象
     * @returns
     */
    acquireObject(): T;
    /**
     * 回收对象到对象池
     * @param object
     */
    recycleObject(object: T): void;
}
