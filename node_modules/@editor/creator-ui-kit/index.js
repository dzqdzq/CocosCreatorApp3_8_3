'use strict';

/** 
 * 根据代码运行进程(主进程，渲染进程)的不同，导出不同进程的模块。
 */

if (process.type === 'browser') {
    module.exports = require('./dist/browser');
} else {
    module.exports = require('./dist/renderer');
}
